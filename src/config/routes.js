const routes = {
  home: '/',
  profileUser: '/profile',
  orderOnline: '/order-online',
  cartDetail: '/cart/:id',
  register: '/register',
  LogIn: '/Log-in',
  notFound: '*',
};

export default routes;
