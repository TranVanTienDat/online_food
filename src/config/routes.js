const routes = {
  home: '/',
  profileUser: '/profile',
  orderOnline: '/order-online',
  cartDetail: '/cart/:id',
  register: '/register',
  LogIn: '/Log-in',
  resetPassword: '/reset-password',
  notFound: '*',
};

export default routes;
